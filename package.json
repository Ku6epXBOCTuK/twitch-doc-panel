{
	"name": "twitch-doc-panel",
	"private": true,
	"type": "module",
	"version": "0.2.0",
	"scripts": {
		"dev": "vite",
		"check": "svelte-check --tsconfig ./tsconfig.json",
		"test": "vitest run",
		"build:bundle": "vite build -c vite.build.ts",
		"build": "npm run build:bundle && node scripts/pack.ts",
		"build:fixtures": "node builder/build.ts fixtures",
		"install-cli": "node scripts/install-cli.ts",
		"assets": "node scripts/assets.ts",
		"screenshots": "node scripts/screenshot.ts",
		"format": "prettier --write . --log-level=warn"
	},
	"dependencies": {
		"gray-matter": "^4.0.3",
		"marked": "^12.0.0"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^5.0.0",
		"@types/node": "^26.6.2",
		"prettier": "^3.9.8",
		"prettier-plugin-svelte": "^4.1.1",
		"sirv": "^3.0.2",
		"svelte": "^5.0.0",
		"svelte-check": "^4.7.6",
		"typescript": "^5.9.3",
		"vite": "^6.0.0",
		"vitest": "^5.0.1"
	}
}
